/*global describe, it*/

'use strict';

describe('api', function () {
    describe('GET /api/users', function () {
        it('respond with an array of users', function () {
            var a = 1;
            a.should.equal(1);
        });
    });
});