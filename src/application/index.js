import header from './header';
import bar from './bar';
import cartridge from './cartridge';
import menu from './menu';
import popin from './popin';
import confirmationPopin from './confirmation-popin';
import messageCenter from './message-center';
import errorCenter from './error-center';
import contentBar from './content-bar';
import contentActions from './content-actions';
import layout from './layout';
import loadingBar from './loading-bar';

export {
    header, bar, cartridge, menu, popin, confirmationPopin, messageCenter,
    errorCenter, contentBar, contentActions, layout, loadingBar
};
export default {
    header, bar, cartridge, menu, popin, confirmationPopin, messageCenter,
    errorCenter, contentBar, contentActions, layout, loadingBar
};
